# so in tkinter everything is a widget ther are button text and many other widgets including frame widgets 
#the first ting that is created is the root widget in this case is the main window of the application interface.

from tkinter import * 